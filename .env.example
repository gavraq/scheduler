# API key for authenticating with the Mac agent service (telegram-agent)
# Must match the AGENT_API_KEY in the telegram-agent .env
AGENT_API_KEY=your-api-key-here

# Optional: API key for protecting scheduler management endpoints (run-now)
# If not set, management endpoints are unprotected
SCHEDULER_API_KEY=
